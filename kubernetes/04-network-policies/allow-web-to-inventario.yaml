apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-web-to-inventario
  namespace: inventario
spec:
  podSelector:
    matchLabels:
      app: inventario-db
  policyTypes: [Ingress]
  ingress:
    - from:
        - podSelector: { matchLabels: { app: inventario-web } }
      ports:
        - { port: 27017, protocol: TCP }
