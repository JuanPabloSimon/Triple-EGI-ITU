apiVersion: v1
kind: Service
metadata:
  name: ubicacion-db
  namespace: inventario
spec:
  type: ClusterIP
  selector:
    app: ubicacion-db
  ports:
    - port: 1433
      targetPort: 1433
      protocol: TCP
